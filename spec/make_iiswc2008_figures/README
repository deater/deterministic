These were the C code and scripts used to generate the plots
for the IISWC08 paper and related tech report.

The data from ../iiswc2008 was used, but the directory structure was
a bit different in my setup at the time so these scripts currently 
won't work without some changes.

An (incomplete) list of what programs generated which figures:
	make_percent_graphs.sh  -- Figures 1 and 2 (coefficient of var)


